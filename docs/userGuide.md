# User Guide
To use the Static-Flow-Audit scripts, follow these steps:
1. Place your Python code in a string or a separate file.
2. Run the desired detector from the `/scripts` directory.
   Example: `python scripts/unused_function_detector.py`
3. View the console output for identified dead code and line numbers.
